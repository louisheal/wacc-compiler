#library function that takes a char and returns true if it is a lower case, false otherwise

begin
  bool isLower(char c) is
    int x = ord(c);
    if(x >= 97 && x <= 122)
    then
      return true
    else
      return false
    fi 
  end
  skip
end