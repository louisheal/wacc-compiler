#library function that takes a char and returns true if it is a space, false otherwise

begin
  bool isSpace(char c) is
    int x = ord(c);
    if (x == 32)
    then
      return true
    else
      return false
    fi 
  end
  skip
end