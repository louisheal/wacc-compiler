#library function that takes an char and returns true if it is an ascii charecter, false otherwise

begin
  bool isAscii(char c) is
    int x = ord(c);
    if ((x >= 0 && x <= 127))
    then
      return true
    else
      return false
    fi 
  end
  skip
end